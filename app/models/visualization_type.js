const VISUALIZATION_TYPES = ['table', 'column', 'bar', 'line'];

const VISUALIZATION_TYPE_OBJECTS = VISUALIZATION_TYPES.map(type => ({ enabled: true, type }));

export default VISUALIZATION_TYPE_OBJECTS;
