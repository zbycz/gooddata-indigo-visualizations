import '../styles/dashboard';
import '../styles/dashboard_edit';
import React, { Component } from 'react';

export default class EditorHeader extends Component {
    render() {
        return (
            <div className="adi-editor-header">
                This is editor header!
            </div>
        );
    }
}
